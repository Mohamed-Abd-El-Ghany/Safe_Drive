import 'package:flutter/material.dart';

class Emmergancy extends StatelessWidget {
  const Emmergancy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Emmergancy Screen'),
      ),
    );
  }
}